"""Brand visual theme — palette + matplotlib rcParams + helpers.

Importable from analysis scripts so the brand stays consistent across
every figure in the paper::

    from theme.mpl_theme import set_brand_style, BRAND_COLORS
"""
